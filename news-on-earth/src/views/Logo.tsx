import React from 'react';
import logo from '../assets/logo.svg'; // ロゴファイルをインポート

const Logo = () => {
  return (
    <div>
      <img src={logo} alt="Logo" />
    </div>
  );
};

export default Logo;
